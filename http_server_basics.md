# Простой HTTP-сервер на Ubuntu 24.04 для новичков

## 📋 Что мы будем делать
Создадим простейший HTTP-сервер для раздачи файлов по сети

## 🔧 Шаг 1: Подготовка системы

### Обновляем пакеты
```bash
sudo apt update
sudo apt upgrade -y
```

### Проверяем наличие Python3
```bash
python3 --version
```
*(Обычно Python3 уже предустановлен в Ubuntu 24.04)*

## 🚀 Шаг 2: Создаем рабочую директорию

```bash
# Создаем папку для файлов
mkdir ~/my-http-server
cd ~/my-http-server

# Создаем тестовые файлы для демонстрации
echo "Hello World! Это тестовый файл." > test.txt
echo "<html><body><h1>Мой первый сервер!</h1></body></html>" > index.html
```

## 🌐 Шаг 3: Запускаем HTTP-сервер

### Вариант A: Простой запуск (для временного использования)
```bash
# Запускаем сервер в текущей директории
python3 -m http.server 8080
```

### Вариант B: Запуск в фоне (для постоянной работы)
```bash
# Запускаем в фоне
cd ~/my-http-server
nohup python3 -m http.server 8080 > server.log 2>&1 &

# Проверяем что сервер работает
ps aux | grep python3
```

### Вариант C: Создаем скрипт для удобства
```bash
# Создаем скрипт запуска
nano ~/start-server.sh
```

Добавляем в файл:
```bash
#!/bin/bash
echo "Запуск HTTP-сервера на порту 8080..."
echo "Файлы доступны по адресу: http://ваш-ip:8080"
cd ~/my-http-server
python3 -m http.server 8080
```

Делаем скрипт исполняемым:
```bash
chmod +x ~/start-server.sh
```

## 🔍 Шаг 4: Проверяем работу сервера

### На самой Ubuntu:
```bash
# Проверяем локально
curl http://localhost:8080

# Или в браузере
firefox http://localhost:8080
```

### Узнаем IP адрес для доступа с других устройств:
```bash
ip addr show
# Ищите строки с "inet" - обычно это что-то типа 192.168.1.XXX
```

## 📡 Шаг 5: Как получить файлы с другого компьютера

### Если клиент - Linux/Mac:
```bash
# Скачать файл
wget http://IP-АДРЕС-УБУНТУ:8080/test.txt

# Или посмотреть содержимое
curl http://IP-АДРЕС-УБУНТУ:8080/
```

### Если клиент - Windows:
```bash
# В PowerShell
Invoke-WebRequest -Uri "http://IP-АДРЕС-УБУНТУ:8080/test.txt" -OutFile "test.txt"

# Или в браузере просто перейдите по адресу:
# http://IP-АДРЕС-УБУНТУ:8080
```

## ⚡ Шаг 6: Полезные команды управления

### Остановить сервер:
```bash
# Найти процесс сервера
ps aux | grep "http.server"

# Остановить (замените PID на реальный номер)
kill PID
```

### Проверить логи (если запускали через nohup):
```bash
tail -f ~/my-http-server/server.log
```

### Добавить файлы для раздачи:
```bash
# Просто копируйте файлы в папку
cp /путь/к/файлу ~/my-http-server/

# Или создавайте новые
echo "Новый файл" > ~/my-http-server/newfile.txt
```

## 🛡️ Шаг 7: Если не работает - решение проблем

### Проверка firewall:
```bash
# Проверить статус firewall
sudo ufw status

# Если включен, разрешить порт 8080
sudo ufw allow 8080
```

### Проверка что сервер слушает правильный интерфейс:
```bash
# Какие порты слушает система
sudo netstat -tlnp | grep 8080
```

### Если порт занят:
```bash
# Используйте другой порт
python3 -m http.server 8081
```

## 📝 Пример использования (как в вашем задании)

### На сервере (Ubuntu):
```bash
# 1. Переходим в папку с файлами
cd /tmp

# 2. Запускаем сервер
python3 -m http.server 8080
```

### На клиенте (другая машина):
```bash
# Скачиваем файл
wget http://IP-СЕРВЕРА:8080/avml
chmod +x avml
```

## 💡 Советы для новичков

1. **Безопасность**: Этот сервер открывает доступ ко ВСЕМ файлам в папке и ее подпапках
2. **Только для локальной сети**: Не используйте для доступа из интернета
3. **Временное решение**: Для продакшена используйте nginx/apache
4. **Логи**: Все запросы будут видны в консоли где запущен сервер

## 🎯 Краткая шпаргалка

```bash
# ЗАПУСК СЕРВЕРА:
cd /папка/с/файлами
python3 -m http.server 8080

# ОСТАНОВКА:
Ctrl+C

# ПРОВЕРКА:
curl http://localhost:8080
```

Теперь у вас работает простой HTTP-сервер! Можете добавлять файлы в папку `~/my-http-server` и они будут доступны по сети.